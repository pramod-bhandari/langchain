# Supabase configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# OpenAI API configuration
# Note: DO NOT prefix with NEXT_PUBLIC_ to keep API keys secure!
# The OPENAI_API_KEY is accessed on the server side only for security.
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL_NAME=gpt-4-turbo
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Optional: For production-grade token security
JWT_SECRET=your_secure_secret_for_jwt_tokens

# Copy this file to .env.local and replace placeholder values with actual credentials
# DO NOT commit your .env.local file to version control

# IMPORTANT: In Next.js, variables with NEXT_PUBLIC_ prefix are exposed to the browser.
# Variables without this prefix are only available on the server side.
# This is important for keeping API keys and other secrets secure. 